# Copy to .env and adjust. Note: these are secrets sitting in a plaintext file
# that both compose and the app read — convenient, but not real secret
# management. Keeping dev and prod values in sync is on you.
POSTGRES_PASSWORD=postgres
SIGNING_KEY=dev-demo-key
